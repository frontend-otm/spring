$(document).on("ready", function () {
  $(".spring__dayly__slider__slider").slick({
    arrows: false,
    dots: true,
    infinite: true,
    slidesToShow: 1,
  });
});

$(document).on("ready", function () {
  $(".graph__belle__slider__slider").slick({
    arrows: false,
    dots: true,
    infinite: true,
    slidesToShow: 1,
  });
});